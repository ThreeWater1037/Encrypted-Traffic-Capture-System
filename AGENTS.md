# 浏览器采集维护约定

- Chrome、Edge、Firefox 是同等支持的采集浏览器。修改网络等待、资源屏蔽、缓存控制、TLS 密钥、PCAP 边界、代理、失败判定、诊断输出或退出清理时，必须同步检查并更新三种浏览器；不能只修改 Chromium 分支。
- 尽量复用共同的请求账本、完成判断和输出格式。浏览器协议差异放在适配层：Chrome/Edge 使用 CDP，Firefox 使用 WebDriver BiDi。
- 新增或修复采集行为时补充对应回归测试，并覆盖 Firefox；涉及协议或生命周期时运行可用的真实浏览器本地测试。无法实测的浏览器、平台或场景必须在交付中明确标记，不能把配置声明当作验证结果。
- 如果某个浏览器不能提供等价能力，明确记录具体差异并显式失败或给出已说明的替代策略，禁止静默退化为固定等待后仍声称完成网络观测。
- 当前 Firefox 在独立临时 Profile 中禁用 Service Worker；Chrome/Edge 绕过 Service Worker。维护时保留这项差异说明和对应验证。
