# Wiki Fetcher 使用说明

使用真实浏览器引擎（Selenium）访问 Wiki 页面，强制全量网络请求，并可捕获 TLS 会话密钥与完整网络流量。

---

## 环境要求

| 依赖 | 说明 |
|------|------|
| Python 3.10+ | 需支持 `match` 语法与类型注解 |
| selenium | `pip3 install selenium webdriver-manager` |
| Google Chrome | 已安装即可，ChromeDriver 自动下载 |
| Microsoft Edge（可选）| Windows 通常已预装，EdgeDriver 自动下载 |
| Firefox（可选）| 从 [mozilla.org/firefox](https://www.mozilla.org/firefox/) 安装 |
| Safari（可选）| macOS 自带，需先运行 `safaridriver --enable` |
| tshark（可选）| 安装 [Wireshark](https://www.wireshark.org/) 即附带，用于 `--pcap` 功能 |

---

## 安装

```bash
pip3 install selenium webdriver-manager
```

---

## 快速开始

```bash
# 访问单个 URL（默认使用 Chrome + Firefox）
python3 wiki_fetcher.py "https://en.wikipedia.org/wiki/Python_(programming_language)"

# 从 txt 文件批量访问，同时抓包
python3 wiki_fetcher.py --input urls.txt --pcap
```

---

## 参数说明

```
usage: wiki_fetcher.py [-h] [--input FILE] [--output-dir DIR]
                       [--browsers {chrome,edge,firefox,safari} [...]]
                       [--pcap]
                       [url]
```

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `url` | 位置参数 | — | 单个 URL，与 `--input` 二选一 |
| `--input / -i` | 文件路径 | — | TSV 文件，每行格式为 `ID<TAB>名称<TAB>完整URL`；`#` 开头为注释，空行忽略 |
| `--output-dir` | 目录路径 | `./fetch_output` | 输出根目录，每个 URL 自动创建子目录 |
| `--browsers` | 多选 | `chrome firefox` | 使用的浏览器，可选 `chrome` `edge` `firefox` `safari` |
| `--pcap` | 开关 | 关闭 | 捕获每次访问的完整网络流量并保存为 pcap |

> **间隔时间**：批量访问时两个 URL 之间的等待秒数，在脚本顶部修改：
> ```python
> INTERVAL_BETWEEN_URLS: float = 3.0   # 单位：秒
> ```

---

## URL 输入文件格式（urls.txt）

```
# 这是注释，会被忽略
1	Python	https://en.wikipedia.org/wiki/Python_(programming_language)
2	TCP	https://en.wikipedia.org/wiki/Transmission_Control_Protocol
3	HTTPS	https://en.wikipedia.org/wiki/HTTPS
```

其中字段之间必须使用制表符（Tab），不能使用普通空格。子机器 Worker Flask
接收结构化 JSON 后会自动生成该文件。

---

## 使用示例

```bash
# 1. 访问单个 URL，仅用 Chrome
python3 wiki_fetcher.py "https://en.wikipedia.org/wiki/Python" \
    --browsers chrome

# 2. 访问单个 URL，Chrome + Firefox，同时抓包
python3 wiki_fetcher.py "https://en.wikipedia.org/wiki/Python" \
    --browsers chrome firefox --pcap

# 3. 从 txt 文件批量访问，Chrome + Safari，自定义输出目录
python3 wiki_fetcher.py --input urls.txt \
    --browsers chrome safari \
    --output-dir ./results

# 4. 批量访问 + 全部三种浏览器 + 抓包
python3 wiki_fetcher.py --input urls.txt \
    --browsers chrome firefox safari \
    --pcap --output-dir ./results
```

---

## 输出目录结构

每个 URL 生成一个独立子目录，按访问序号和页面路径自动命名：

```
fetch_output/
├── 001_wiki_Python_(programming_language)/
│   ├── body_chrome.html          # Chrome 获取的完整 HTML
│   ├── body_edge.html            # Edge 获取的完整 HTML
│   ├── body_firefox.html         # Firefox 获取的完整 HTML
│   ├── tls_keys_chrome.log       # Chrome TLS 会话密钥（NSS 格式）
│   ├── tls_keys_edge.log         # Edge TLS 会话密钥（NSS 格式）
│   ├── tls_keys_firefox.log      # Firefox TLS 会话密钥（NSS 格式）
│   ├── capture_chrome.pcap       # Chrome 访问期间的完整网络流量
│   ├── capture_edge.pcap         # Edge 访问期间的完整网络流量
│   ├── capture_firefox.pcap      # Firefox 访问期间的完整网络流量
│   └── report.txt                # 本 URL 的访问报告
├── 002_wiki_Transmission_Control_Protocol/
│   └── ...
└── summary.txt                   # 所有 URL 的汇总报告
```

---

## TLS 会话密钥

- **格式**：NSS Key Log（标准格式，Wireshark 原生支持）
- **包含**：`CLIENT_HANDSHAKE_TRAFFIC_SECRET` / `SERVER_HANDSHAKE_TRAFFIC_SECRET` / `CLIENT_TRAFFIC_SECRET_0` / `SERVER_TRAFFIC_SECRET_0` / `EXPORTER_SECRET`
- **Chrome / Edge**：通过启动参数 `--ssl-key-log-file` 捕获
- **Firefox**：通过环境变量 `SSLKEYLOGFILE` 捕获（NSS 原生支持）
- **Safari**：不支持密钥导出

**在 Wireshark 中解密 HTTPS 流量**：
```
Edit → Preferences → Protocols → TLS
  → (Pre)-Master-Secret log filename → 选择 tls_keys_chrome.log
```

---

## pcap 流量捕获

启用 `--pcap` 后，每次访问独立捕获一份 pcap，覆盖完整访问生命周期：

```
tshark 启动（打开 BPF 句柄）
  ↓
浏览器进程启动
  ↓
DNS 查询              ← 包含在 pcap 中
TCP 三次握手          ← 包含在 pcap 中
TLS ClientHello       ← 包含在 pcap 中
TLS 握手完成          ← 包含在 pcap 中
HTTP/2 数据传输       ← 包含在 pcap 中
QUIC/HTTP3（如有）    ← 包含在 pcap 中
页面加载完成
  ↓
浏览器退出
TCP FIN / TLS close_notify ← 包含在 pcap 中
  ↓
tshark 停止 → pcap 落盘
```

**需要 BPF 读取权限**（macOS 上属于 `access_bpf` 组则无需 sudo）：
```bash
# 查看自己是否已有权限
groups | grep access_bpf
```

---

## 缓存禁用机制

每次访问强制全量网络请求，不使用任何缓存：

| 层级 | Chrome | Edge | Firefox |
|------|--------|------|---------|
| HTTP 头 | `Cache-Control: no-cache, no-store` + `Pragma: no-cache`（CDP 注入）| 同 Chrome | 同左 |
| 磁盘缓存 | `--disk-cache-size=0` + `--disable-cache` 启动参数 | 同 Chrome | `browser.cache.disk.enable = false` |
| 内存缓存 | `--disable-application-cache` | 同 Chrome | `browser.cache.memory.enable = false` |
| DevTools | `Network.setCacheDisabled(true)` | 同 Chrome | — |
| 浏览器配置 | 每次使用全新临时 Profile 目录 | 同 Chrome | 同左 |

---

## 常见问题

**Firefox 启动失败**

Firefox 未安装，或不在默认路径。安装后重试，或确认路径：
```bash
ls /Applications/Firefox.app
```

**Safari 无法启动**

需要先启用 safaridriver（需要管理员权限，仅需一次）：
```bash
safaridriver --enable
```

**pcap 为空**

检查 BPF 权限：
```bash
groups | grep access_bpf
# 若不在组内，联系管理员或使用 sudo 运行
```

**tshark 未找到**

安装 [Wireshark](https://www.wireshark.org/)（含 tshark），或确认路径：
```bash
ls /Applications/Wireshark.app/Contents/MacOS/tshark
```

---

## 子机器 Worker Flask

多机器模式下，子机器通过 `5100/TCP` 提供内部任务 API，后台串行调用
`wiki_fetcher.py` 和 `batch_process.py`，实验结果保存在子机器本地。

安装、配置、接口和调用示例见：[`worker_agent/README.md`](worker_agent/README.md)。

---

## 主控 Flask 与 Vue 页面

本机多服务模式固定使用以下端口：

```text
Vue 前端 5173 → 主控 Flask 5200 → Worker Agent 5100
```

三个 PowerShell 窗口依次启动：

```powershell
# 首次配置：复制示例并在 worker.yaml 中填写随机 Token、机器 ID 和路径
Copy-Item worker.yaml.example worker.yaml
notepad worker.yaml

# 窗口 1：Worker 自动读取根目录 worker.yaml
python -m worker_agent

# 窗口 2：主控 Token 必须与 worker.yaml 中的 worker.token 相同
$env:BOOTSTRAP_LOCAL_WORKER='true'
$env:LOCAL_WORKER_TOKEN='与 worker.yaml 相同的随机长字符串'
python -m master_server

# 窗口 3：前端
cd frontend
npm run dev
```

Worker 配置优先级为“环境变量 > `worker.yaml` > 默认值”。需要临时覆盖时仍可使用
`WORKER_TOKEN`、`WORKER_PORT` 等原有环境变量；日常启动不再需要逐项设置。
主控的 `BOOTSTRAP_LOCAL_WORKER` 默认为 `false`，机器由页面维护且删除后不会在重启时
恢复；上面的本机三服务联调示例显式设为 `true`，因此会自动注册本机 Worker。

打开 `http://127.0.0.1:5173`。主控数据库默认位于
`master_data/master.db`，上传到主控的 TXT/TSV 位于
`master_data/uploads/{job_id}`；抓包和分析文件仍保存在对应 Worker 本地。

主控配置、接口和迁移说明见：[`master_server/README.md`](master_server/README.md)。
