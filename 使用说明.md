# Wiki Fetcher 使用说明

使用真实浏览器引擎（Selenium）访问 Wiki 页面，强制全量网络请求，并可捕获 TLS 会话密钥与完整网络流量。

---

## 环境要求

| 依赖 | 说明 |
|------|------|
| Python 3.10+ | 需支持 `match` 语法与类型注解 |
| selenium | `pip3 install selenium webdriver-manager` |
| Google Chrome | 已安装即可，ChromeDriver 自动下载 |
| Firefox（可选）| 从 [mozilla.org/firefox](https://www.mozilla.org/firefox/) 安装 |
| Safari（可选）| macOS 自带，需先运行 `safaridriver --enable` |
| tshark（可选）| 安装 [Wireshark](https://www.wireshark.org/) 即附带，用于 `--pcap` 功能 |

---

## 安装

```bash
pip3 install selenium webdriver-manager
```

---

## 快速开始

```bash
# 访问单个 URL（默认使用 Chrome + Firefox）
python3 wiki_fetcher.py "https://en.wikipedia.org/wiki/Python_(programming_language)"

# 从 txt 文件批量访问，同时抓包
python3 wiki_fetcher.py --input urls.txt --pcap
```

---

## 参数说明

```
usage: wiki_fetcher.py [-h] [--input FILE] [--output-dir DIR]
                       [--browsers {chrome,firefox,safari} [...]]
                       [--pcap]
                       [url]
```

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `url` | 位置参数 | — | 单个 URL，与 `--input` 二选一 |
| `--input / -i` | 文件路径 | — | txt 文件，每行一个 URL；`#` 开头为注释，空行忽略 |
| `--output-dir` | 目录路径 | `./fetch_output` | 输出根目录，每个 URL 自动创建子目录 |
| `--browsers` | 多选 | `chrome firefox` | 使用的浏览器，可选 `chrome` `firefox` `safari` |
| `--pcap` | 开关 | 关闭 | 捕获每次访问的完整网络流量并保存为 pcap |

> **间隔时间**：批量访问时两个 URL 之间的等待秒数，在脚本顶部修改：
> ```python
> INTERVAL_BETWEEN_URLS: float = 3.0   # 单位：秒
> ```

---

## URL 输入文件格式（urls.txt）

```
# 这是注释，会被忽略
https://en.wikipedia.org/wiki/Python_(programming_language)
https://en.wikipedia.org/wiki/Transmission_Control_Protocol

# 空行也会被忽略
https://en.wikipedia.org/wiki/HTTPS
```

---

## 使用示例

```bash
# 1. 访问单个 URL，仅用 Chrome
python3 wiki_fetcher.py "https://en.wikipedia.org/wiki/Python" \
    --browsers chrome

# 2. 访问单个 URL，Chrome + Firefox，同时抓包
python3 wiki_fetcher.py "https://en.wikipedia.org/wiki/Python" \
    --browsers chrome firefox --pcap

# 3. 从 txt 文件批量访问，Chrome + Safari，自定义输出目录
python3 wiki_fetcher.py --input urls.txt \
    --browsers chrome safari \
    --output-dir ./results

# 4. 批量访问 + 全部三种浏览器 + 抓包
python3 wiki_fetcher.py --input urls.txt \
    --browsers chrome firefox safari \
    --pcap --output-dir ./results
```

---

## 输出目录结构

每个 URL 生成一个独立子目录，按访问序号和页面路径自动命名：

```
fetch_output/
├── 001_wiki_Python_(programming_language)/
│   ├── body_chrome.html          # Chrome 获取的完整 HTML
│   ├── body_firefox.html         # Firefox 获取的完整 HTML
│   ├── tls_keys_chrome.log       # Chrome TLS 会话密钥（NSS 格式）
│   ├── tls_keys_firefox.log      # Firefox TLS 会话密钥（NSS 格式）
│   ├── capture_chrome.pcap       # Chrome 访问期间的完整网络流量
│   ├── capture_firefox.pcap      # Firefox 访问期间的完整网络流量
│   └── report.txt                # 本 URL 的访问报告
├── 002_wiki_Transmission_Control_Protocol/
│   └── ...
└── summary.txt                   # 所有 URL 的汇总报告
```

---

## TLS 会话密钥

- **格式**：NSS Key Log（标准格式，Wireshark 原生支持）
- **包含**：`CLIENT_HANDSHAKE_TRAFFIC_SECRET` / `SERVER_HANDSHAKE_TRAFFIC_SECRET` / `CLIENT_TRAFFIC_SECRET_0` / `SERVER_TRAFFIC_SECRET_0` / `EXPORTER_SECRET`
- **Chrome**：通过启动参数 `--ssl-key-log-file` 捕获
- **Firefox**：通过环境变量 `SSLKEYLOGFILE` 捕获（NSS 原生支持）
- **Safari**：不支持密钥导出

**在 Wireshark 中解密 HTTPS 流量**：
```
Edit → Preferences → Protocols → TLS
  → (Pre)-Master-Secret log filename → 选择 tls_keys_chrome.log
```

---

## pcap 流量捕获

启用 `--pcap` 后，每次访问独立捕获一份 pcap，覆盖完整访问生命周期：

```
tshark 启动（打开 BPF 句柄）
  ↓
浏览器进程启动
  ↓
DNS 查询              ← 包含在 pcap 中
TCP 三次握手          ← 包含在 pcap 中
TLS ClientHello       ← 包含在 pcap 中
TLS 握手完成          ← 包含在 pcap 中
HTTP/2 数据传输       ← 包含在 pcap 中
QUIC/HTTP3（如有）    ← 包含在 pcap 中
页面加载完成
  ↓
浏览器退出
TCP FIN / TLS close_notify ← 包含在 pcap 中
  ↓
tshark 停止 → pcap 落盘
```

**需要 BPF 读取权限**（macOS 上属于 `access_bpf` 组则无需 sudo）：
```bash
# 查看自己是否已有权限
groups | grep access_bpf
```

---

## 缓存禁用机制

每次访问强制全量网络请求，不使用任何缓存：

| 层级 | Chrome | Firefox |
|------|--------|---------|
| HTTP 头 | `Cache-Control: no-cache, no-store` + `Pragma: no-cache`（CDP 注入）| 同左 |
| 磁盘缓存 | `--disk-cache-size=0` + `--disable-cache` 启动参数 | `browser.cache.disk.enable = false` |
| 内存缓存 | `--disable-application-cache` | `browser.cache.memory.enable = false` |
| DevTools | `Network.setCacheDisabled(true)` | — |
| 浏览器配置 | 每次使用全新临时 Profile 目录 | 同左 |

---

## 常见问题

**Firefox 启动失败**

Firefox 未安装，或不在默认路径。安装后重试，或确认路径：
```bash
ls /Applications/Firefox.app
```

**Safari 无法启动**

需要先启用 safaridriver（需要管理员权限，仅需一次）：
```bash
safaridriver --enable
```

**pcap 为空**

检查 BPF 权限：
```bash
groups | grep access_bpf
# 若不在组内，联系管理员或使用 sudo 运行
```

**tshark 未找到**

安装 [Wireshark](https://www.wireshark.org/)（含 tshark），或确认路径：
```bash
ls /Applications/Wireshark.app/Contents/MacOS/tshark
```
